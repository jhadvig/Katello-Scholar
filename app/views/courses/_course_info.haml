%h4.section
	Course Info:


.span6
	-@course.attributes.each do |k,v|
		%div
			%span 
				-if k == "status"
					%strong #{k.capitalize} :
					#{Course::STATUS.key(v)}
				-else
					%strong #{k.capitalize} :
					#{v}

.span6

%br
%br

- if can? :manage, Course
	=link_to "Edit Course", edit_course_path(@course), :class=>"btn btn-info course-edit-path"
